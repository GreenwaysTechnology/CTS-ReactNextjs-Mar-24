
//literal objects - no class,no function

const employee = {
    id: 1,
    name: 'Subramanian',
    salary: 1000,
    //methods
    calculate() {
        return 1000
    }
}
console.log(employee.id,employee.name,employee.salary,employee.calculate())